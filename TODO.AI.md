# TODO.AI.md

- AI.md Part 6 "Current hooks" table is missing 10 real hooks in
  home/hooks/: bound-shell-lifetime.sh, drift-guard-read.sh,
  enforce-docker-rm.sh, no-force-push.sh, no-secrets.sh, post-compact.sh,
  no-forbidden-files.sh, spec-guard.sh, spec-guard-mark.sh,
  validate-workflows.sh, block-host-toolchain.sh. Add a row for each
  (trigger + purpose) to bring the table in sync with the actual scripts.
